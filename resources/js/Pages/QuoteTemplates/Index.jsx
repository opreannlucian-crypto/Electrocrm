import AuthenticatedLayout from "@/Layouts/AuthenticatedLayout";
import { Head, Link, router, usePage } from "@inertiajs/react";
export default function QuoteTemplatesIndex({templates=[]}){const {flash}=usePage().props;return <AuthenticatedLayout><Head title="Șabloane oferte"/><div className="mx-auto max-w-6xl px-4 py-8"><div className="mb-6 flex items-center justify-between"><div><h1 className="text-3xl font-bold">Șabloane oferte și devize</h1><p className="mt-1 text-slate-500">Creează un model de ofertă și reutilizează-l oricând.</p></div><div className="flex gap-2"><Link href={route("quotes.index")} className="rounded border px-4 py-2">← Oferte</Link><Link href={route("quote-templates.create",{as_template:1})} className="rounded bg-blue-600 px-4 py-2 font-semibold text-white">＋ Șablon nou</Link></div></div>{flash?.success&&<div className="mb-5 rounded bg-emerald-50 p-4 text-emerald-800">{flash.success}</div>}<div className="grid gap-4 md:grid-cols-2">{templates.map(template=><div key={template.id} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><h2 className="font-bold">{template.name}</h2><p className="mt-1 text-sm text-slate-500">{template.type==="deviz"?"Deviz":"Ofertă"} · {template.items?.length||0} poziții</p></div><span className="rounded bg-blue-50 px-2 py-1 text-xs text-blue-700">TVA {template.vat_rate}%</span></div>{template.title&&<p className="mt-3 text-sm">{template.title}</p>}<div className="mt-5 flex gap-2"><Link href={route("quotes.create",{template_id:template.id})} className="rounded bg-blue-600 px-4 py-2 text-sm font-semibold text-white">Folosește șablonul</Link><button onClick={()=>confirm("Ștergi acest șablon?")&&router.delete(route("quote-templates.destroy",template.id))} className="rounded border border-rose-600 px-4 py-2 text-sm text-rose-700">Șterge</button></div></div>)}{templates.length===0&&<div className="rounded-2xl border bg-white p-8 text-slate-500 md:col-span-2">Nu ai încă șabloane. Apasă „＋ Șablon nou” pentru a crea primul model de ofertă.</div>}</div></div></AuthenticatedLayout>}
